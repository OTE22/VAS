# Tested in isolation with the existing NumPy 1.x model artifacts.
mlflow==3.16.0
optuna==4.9.0
shap==0.49.1
matplotlib>=3.9,<4
numpy>=2.3,<3   # kept in step with requirements-base.txt (numpy 2 since 2026-09-06)
