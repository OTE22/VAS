# CPU-only requirements
# This file is used when no GPU is detected

# Core ML/CV Dependencies (CPU)
# Pinned: an unpinned runtime resolves to whatever is newest at build
# time, so two builds of the same commit can ship different inference
# engines. requirements-cpu.lock.txt pins the full transitive set.
onnxruntime==1.23.2
opencv-python-headless==4.9.0.80
numpy==1.26.4
scikit-image
faiss-cpu

# Web Framework - Production Ready
fastapi
uvicorn[standard]
python-multipart
websockets

# Database & Caching
sqlalchemy
asyncpg
redis
aioredis
pgvector>=0.2.4  # PostgreSQL vector similarity search

# Background Tasks & Queues
celery
kombu

# Monitoring & Logging
prometheus-client
python-json-logger

# Performance
orjson

# Utils
python-dotenv
pydantic
pydantic-settings

# Core Web Framework
gunicorn

# ASGI Server
uvloop
httptools

# Database (Async PostgreSQL)
psycopg2-binary
alembic

# Redis Cache
redis[asyncio]
hiredis

# Image Processing
# opencv-contrib-python removed: it installs a SECOND OpenCV distribution
# alongside opencv-python-headless==4.9.0.80 above. Both provide cv2, so
# the effective version depended on installation order — the running
# container had headless 4.9.0.80 pinned but contrib 4.11.0.86 shadowing it.
Pillow==12.1.0

# Face Recognition Models (CPU)
# (onnxruntime is pinned once at the top of this file)
scikit-image

# Utilities
python-multipart
websockets
aiofiles
httpx

# Security
python-jose[cryptography]
passlib[bcrypt]
PyJWT
bcrypt

# Logging
python-json-logger
colorlog

# LangChain & SQL Agent
langchain
langchain-ollama
langchain-community
langgraph
langchain-experimental

# SQL Agent specific
sqlalchemy-utils
chromadb
fuzzywuzzy[speedup]
sentence-transformers  # For query embedding generation (semantic search)

# Vana AI
pandas
vanna[chromadb,postgres,ollama]
requests

# Document Export (PDF & Word)
reportlab
python-docx

# Identity Clustering (for merge suggestions)
scikit-learn>=1.3.0

# Map Generation (for intelligence tracking)
folium>=0.15.0
offline-folium>=0.1.0  # Completely offline maps - no internet required
tqdm>=4.66.0  # Progress bars for tile download script

