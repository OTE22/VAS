# Home page presentation refresh — 2026-09-22

The home page now leads with a larger service header and direct links to live feeds and identity search. Operational figures appear before workspace actions; search, watchlists, and intelligence have descriptive shortcuts. A section navigation row links to the overview, workspace, health, pipelines, and retention panels. Existing data, permissions, upload actions, and polling remain unchanged.

The scoped stylesheet adds responsive cards, distinct primary/secondary statistics, improved spacing and headings, keyboard focus, and reduced-motion support. A hidden-state correction prevents component display rules from revealing the stale-data badge and pipeline detail before they are needed. The stylesheet URL is versioned `workspace-2`.

Validation: 13 existing home-page static contract checks passed. Chromium fixture checks passed at 1440, 768, and 390px widths: no horizontal overflow, valid section anchors, pipeline selection, hidden freshness/detail elements, refresh failure with previous data retained, and no browser errors. Browser fixtures exclude shared navbar/upload loader scripts; their markup, script order, and existing hooks were preserved. Screenshots and the fixture probe are under `logs/home-ui-20260922/`.

Only `frontend/home.html` and `frontend/css/home-workspace.css` were changed. No backend or database changes. Deployed in production API image `9d0d0b390428`. Previous image and stopped container retained under `before-home-ui-20260922`. Deployed HTML/CSS hashes and the public stylesheet match the workspace; runtime environment, mounts, and device requests were verified unchanged.

Post-deployment authenticated checks returned HTTP 200 for `/home`, `/api/stats`, and `/api/dashboard/pipelines`. The public `/health` endpoint reported healthy, including database, cache, queue, models, tracker, SQL agent, and vector backend.
