# 99a — API index (generated by scripts/generate_api_index.py)

Columns: METHOD path handler [read|WRITE][ audit] deps=[auth/db dependencies]; second line = docstring. `WRITE` = the handler changes the database. Paths are as declared on the router (see each file for its prefix, e.g. sql_agent routes are under /api/sql-agent, settings under /api/settings).

### `backend/routes/__init__.py`

```
```

### `backend/routes/admin_tutorial.py`

```
GET    /tutorial                                               get_admin_tutorial                         [read] deps=["require_role(['admin']", 'get_db']
       Get comprehensive tutorial and learning materials for admin users.
GET    /tutorial/examples                                      get_api_examples                           [read audit] deps=["require_role(['admin']"]
       Get practical code examples for all admin API endpoints.
```

### `backend/routes/advanced_search.py`

```
POST   /api/search/advanced                                    advanced_search                            [read] deps=['get_db', 'require_admin(', 'require_search_csrf']
       Perform advanced multi-face search with quality scoring and watchlist checking.
GET    /api/search/config                                      get_search_config                          [read] deps=['require_admin(']
       Get search configuration.
POST   /api/search/quality-check                               check_image_quality                        [read] deps=['require_admin(', 'require_search_csrf']
       Check image quality without searching.
```

### `backend/routes/audit.py`

```
POST   /api/audit/chatbot                                      record_chatbot_question                    [WRITE audit] deps=['get_db', 'get_current_user']
       Record a LAF-AI question in the same table the Audit Log page reads.
GET    /api/audit/chatbot                                      get_chatbot_audit_logs                     [WRITE audit] deps=['get_db', "require_role(['admin']"]
       Get chatbot audit logs (admin only).
GET    /api/audit/chatbot/stats                                get_chatbot_audit_stats                    [WRITE audit] deps=['get_db', "require_role(['admin']"]
       Get chatbot audit log statistics (admin only).
GET    /api/audit/chatbot/{log_id}                             get_chatbot_audit_log                      [WRITE audit] deps=['get_db', "require_role(['admin']"]
       Get a specific audit log entry by ID (admin only)
```

### `backend/routes/auth.py`

```
POST   /api/auth/login                                         login                                      [read audit] deps=['get_db']
       Authenticate and establish a session.
GET    /api/auth/me                                            get_current_user_info                      [read] deps=['get_current_user_allow_pending_rotation']
       Get current user information (never cached).
GET    /api/auth/me/privileges                                 get_user_privileges                        [WRITE] deps=['get_current_user', 'get_db']
       Get comprehensive user privileges and access information.
POST   /api/auth/logout                                        logout                                     [read audit] deps=['get_current_user_allow_pending_rotation']
       End the session for real.
POST   /api/auth/change-password                               change_password                            [WRITE audit] deps=['require_auth_csrf', 'get_current_user_allow_pending_rotation', 'get_db']
       Change your own password, and take ownership of the account.
```

### `backend/routes/batch_export.py`

```
POST   /api/search/batch                                       batch_search                               [read] deps=['get_db', 'require_admin(', 'require_search_csrf', "rate_limited('batch_search', heavy=True"]
       Perform batch face search across multiple images.
POST   /api/search/export                                      export_search_results                      [read] deps=['require_admin(', 'require_search_csrf', "rate_limited('export', heavy=True"]
       Export search results.
POST   /api/search/batch/export                                export_batch_results                       [read] deps=['require_admin(', 'require_search_csrf', "rate_limited('export', heavy=True"]
       Export batch search results.
GET    /api/search/history                                     get_search_history                         [WRITE] deps=['get_db', 'require_admin(']
       Get search history.
DELETE /api/search/history                                     clear_search_history                       [WRITE] deps=['get_db', 'require_admin(', 'require_search_csrf']
       Clear the caller's search history.
GET    /api/search/history/export                              export_search_history                      [WRITE] deps=['get_db', 'require_admin(']
       Export search history.
```

### `backend/routes/cache.py`

```
GET    /api/cache/stats                                        get_cache_stats                            [read] deps=[]
       Get detailed cache statistics.
POST   /api/cache/warm/{pipeline_id}                           warm_cache_for_pipeline                    [WRITE] deps=['get_db']
       Warm cache for a specific pipeline
POST   /api/cache/clear                                        clear_cache                                [WRITE] deps=[]
       Clear cache (with safety limits)
GET    /api/cache/health                                       cache_health                               [read] deps=[]
       Detailed cache health check
GET    /api/cache/redis/test                                   test_redis_connection                      [WRITE] deps=[]
       Test Redis connection and functionality
GET    /api/cache/redis/stats                                  get_redis_stats                            [read] deps=[]
       Get detailed Redis statistics and cache usage
```

### `backend/routes/conversations.py`

```
GET    /conversations                                          list_conversations                         [WRITE] deps=['require_capability(Capability.CHATBOT_HISTORY_READ', 'get_db']
       The caller's own conversations in the default workspace, pinned first then most recently active, with optional
POST   /conversations                                          create_conversation                        [WRITE] deps=['require_capability(Capability.CHATBOT_USE', 'get_db', "rate_limited('chatbot', heavy=False"]
       Create a conversation and its primary branch; returns the conversation with primary_branch_id.
GET    /conversations/{conversation_id}/messages               get_messages                               [read] deps=['require_capability(Capability.CHATBOT_HISTORY_READ', 'get_db']
       Messages of one branch (the primary branch when branch_id is omitted), oldest first, cursor-paginated via befo
GET    /conversations/{conversation_id}/branches               list_branches                              [read] deps=['require_capability(Capability.CHATBOT_HISTORY_READ', 'get_db']
       All branches of the conversation in creation order, each with its fork point and primary flag.
PATCH  /conversations/{conversation_id}                        rename_conversation                        [WRITE] deps=['require_capability(Capability.CHATBOT_USE', 'get_db']
       Rename the caller's conversation. An all-whitespace title leaves the existing one unchanged.
PATCH  /conversations/{conversation_id}/flags                  set_flags                                  [WRITE] deps=['require_capability(Capability.CHATBOT_USE', 'get_db']
       Set the pinned and/or archived flags; omitted fields are left unchanged.
DELETE /conversations/{conversation_id}                        delete_conversation                        [WRITE] deps=['require_capability(Capability.CHATBOT_HISTORY_DELETE', 'get_db']
       Soft delete: stamps deleted_at and hides the conversation from listings. No rows are removed.
POST   /conversations/{conversation_id}/branches               branch_from_message                        [WRITE] deps=['require_capability(Capability.CHATBOT_USE', 'get_db', "rate_limited('chatbot', heavy=False"]
       Fork the conversation at an earlier user message: copies the history before that message into a new branch and
POST   /conversations/{conversation_id}/feedback               record_feedback                            [WRITE] deps=['require_capability(Capability.CHATBOT_USE', 'get_db']
       Rate a message +1/-1 with an optional comment. One feedback row per (message, user) — repeated calls update it
```

### `backend/routes/dashboard.py`

```
GET    /dashboard                                              dashboard                                  [read] deps=['require_strict_access(allowed_roles=None, require_pipeline_a']
       Production Dashboard HTML page - STRICT AUTHENTICATION REQUIRED
GET    /home                                                   home                                       [WRITE] deps=['get_db']
       Home page - Admin only
GET    /admin/unknown                                          unknown_faces                              [WRITE] deps=['get_db']
       Unknown faces management page.
GET    /admin/identity/{identity_id}                           identity_profile                           [WRITE] deps=['get_db']
       Identity profile page — the app's first path-parameter HTML route.
GET    /signin                                                 signin                                     [read] deps=[]
       Sign in page
GET    /change-password                                        change_password_page                       [read] deps=['get_current_user_allow_pending_rotation']
       Change-password page.
GET    /admin/users                                            admin_users                                [read] deps=["require_strict_access(allowed_roles=['admin'], require_pipel"]
       Admin user management page - ADMIN ONLY
GET    /admin/background-tasks                                 admin_background_tasks                     [WRITE] deps=['get_db']
       Background tasks monitoring page - accessible to all users
GET    /admin/audit                                            admin_audit                                [read audit] deps=["require_strict_access(allowed_roles=['admin'], require_pipel"]
       Admin chatbot audit log page - ADMIN ONLY
GET    /admin/pipelines                                        admin_pipelines                            [read] deps=["require_strict_access(allowed_roles=['admin'], require_pipel"]
       Admin pipeline management page - ADMIN ONLY
GET    /admin/tutorial                                         admin_tutorial                             [read] deps=["require_strict_access(allowed_roles=['admin'], require_pipel"]
       Admin tutorial and learning guide page - ADMIN ONLY
GET    /admin/settings                                         admin_settings                             [read] deps=["require_strict_access(allowed_roles=['admin'], require_pipel"]
       Admin settings management page - ADMIN ONLY
GET    /admin/search                                           admin_search                               [read] deps=["require_strict_access(allowed_roles=['admin'], require_pipel"]
       Advanced Search Intelligence page - ADMIN ONLY
GET    /admin/search-history                                   admin_search_history                       [read] deps=["require_strict_access(allowed_roles=['admin'], require_pipel"]
       Search History page - ADMIN ONLY
GET    /admin/intelligence                                     admin_intelligence                         [read] deps=["require_strict_access(allowed_roles=['admin'], require_pipel"]
       Intelligence Analysis page - ADMIN ONLY
GET    /admin/security-intelligence                            admin_security_intelligence                [read] deps=["require_strict_access(allowed_roles=['admin'], require_pipel"]
       Security Intelligence page - ADMIN ONLY
GET    /admin/watchlists                                       admin_watchlists                           [read] deps=["require_strict_access(allowed_roles=['admin'], require_pipel"]
       Watchlist management page - ADMIN ONLY
GET    /admin/ml-ops                                           admin_ml_ops                               [read] deps=["require_strict_access(allowed_roles=['admin'], require_pipel"]
       ML Operations page — anomaly pipeline, shadow evaluation, drift, labels.
GET    /admin/ml-model                                         admin_ml_model                             [read] deps=["require_strict_access(allowed_roles=['admin'], require_pipel", 'get_db']
       ML Similarity Model management page - ADMIN ONLY (strict auth).
GET    /admin/live-alerts                                      admin_live_alerts                          [WRITE] deps=['get_db']
       Live alerts management page.
GET    /admin/ingest-credentials                               ingest_credentials_page                    [read] deps=["require_strict_access(allowed_roles=['admin'], require_pipel"]
       Ingest credential management page - ADMIN ONLY
GET    /admin/logs                                             logs_page                                  [read] deps=["require_strict_access(allowed_roles=['admin'], require_pipel"]
       Error logs viewer page - ADMIN ONLY
GET    /tracking-people                                        tracking_people                            [read] deps=['require_chatbot_access(']
       People Tracking Intelligence page (requires chatbot access).
GET    /                                                       root                                       [read] deps=[]
       Redirect root to signin page - all users must sign in first
GET    /docs/overview                                          api_overview                               [read] deps=[]
       Comprehensive API overview with examples for all endpoints
GET    /ping                                                   ping                                       [read] deps=[]
       Constant {"status": "ok"} with no authentication and no I/O — the cheapest possible reachability probe.
```

### `backend/routes/detections.py`

```
GET    /api/detections                                         get_all_detections                         [WRITE] deps=['get_db', 'get_current_user']
       Get recent detections - filtered by user's pipeline access
GET    /api/detections/{pipeline_id}                           get_pipeline_detections                    [WRITE] deps=['get_db', 'require_pipeline_access']
       Get detections for specific pipeline with face details
```

### `backend/routes/enrollment_review.py`

```
POST   /api/enrollment/confirm                                 confirm_enrollment                         [WRITE audit] deps=["require_role(['admin']", 'require_upload_csrf', 'get_db']
       Apply an administrator's decision to an upload awaiting review.
POST   /api/enrollment/cancel                                  cancel_enrollment                          [read] deps=["require_role(['admin']", 'require_upload_csrf', 'get_db']
       Drop a parked upload and its file. Nothing was ever created for it.
```

### `backend/routes/health.py`

```
GET    /health/live                                            liveness_check                             [read] deps=[]
       LIVENESS: zero I/O, answers instantly if the event loop is alive.
GET    /health/ready                                           readiness_check                            [read] deps=[]
       READINESS: parallel dependency checks, each bounded to 2 seconds.
GET    /health                                                 health_check                               [read] deps=[]
       Health check endpoint for monitoring
GET    /api/health/offline-policy                              offline_policy_report                      [read] deps=['_require_admin_dependency(']
       The production startup checklist, [PASS]/[FAIL] per item, for
GET    /health/offline-policy                                  offline_policy_report                      [read] deps=['_require_admin_dependency(']
       The production startup checklist, [PASS]/[FAIL] per item, for
GET    /health/detailed                                        detailed_health_check                      [read] deps=[]
       Detailed health check for all components
```

### `backend/routes/identities.py`

```
GET    /admin/identities/status                                get_identity_service_status                [read] deps=["require_role(['admin']", 'get_db']
       Check the status of the identity management service.
POST   /admin/identities/load-known-faces                      load_known_faces                           [WRITE] deps=["require_role(['admin']", 'get_db']
       Load known faces from storage/faces directory into the Identity system.
GET    /admin/identities/debug/{identity_id}                   debug_identity_recognition                 [WRITE] deps=["require_role(['admin']", 'get_db']
       Debug endpoint to check why an identity is or isn't being recognized.
GET    /admin/identities/verify-indexes                        verify_indexes                             [read] deps=["require_role(['admin']", 'get_db']
       Verify both KNOWN and UNKNOWN FAISS indexes match the database.
GET    /admin/unknown                                          list_unknown_identities                    [WRITE] deps=['get_db', 'get_current_user']
       List unknown identities with pagination and filters.
GET    /admin/identity/{identity_id}                           get_identity_details                       [WRITE] deps=['get_db', 'get_current_user']
       Get detailed information about an identity including timeline (admin only).
GET    /admin/unknown/{identity_id}/match-candidates           unknown_match_candidates                   [WRITE] deps=['get_db', 'get_current_user']
       Suggestions for the promote modal. Looking must never change anything.
POST   /admin/unknown/{identity_id}/promote                    promote_unknown_to_known                   [WRITE audit] deps=['get_db', 'get_current_user']
       Promote an unknown identity to known.
GET    /admin/identities                                       list_all_identities                        [WRITE] deps=['get_db', "require_role(['admin']"]
       List identities for the Intelligence Analysis dropdown.
GET    /admin/identities/search                                search_identities                          [WRITE] deps=['get_db', 'get_current_user']
       Search for identities by ID or display name.
POST   /admin/identities/merge                                 merge_identities                           [WRITE] deps=['get_db', 'get_current_user']
       Merge two identities into one.
POST   /admin/identities/merges/{merge_id}/unmerge             unmerge_identities                         [WRITE] deps=['get_db', "require_role(['admin']"]
       Reverse one merge.
POST   /admin/identities/merge-preview                         preview_merge                              [WRITE] deps=['get_db', 'get_current_user']
       Production-grade merge preview endpoint.
POST   /admin/identities/merge-multiple                        merge_multiple_identities                  [WRITE] deps=['get_db', 'get_current_user']
       Merge multiple identities into one efficiently.
POST   /search/by-image                                        search_by_image                            [WRITE audit] deps=['get_db', "require_role(['admin']"]
       Search for identities by uploading an image (admin only).
GET    /admin/merge-suggestions/pipeline/{pipeline_id}         get_merge_suggestions_for_pipeline         [WRITE] deps=['get_db', 'get_current_user']
       Get merge suggestions for a specific pipeline using DBSCAN clustering.
GET    /admin/merge-suggestions                                get_merge_suggestions                      [WRITE] deps=['get_db', 'get_current_user']
       Get merge suggestions.
POST   /admin/merge-suggestions/{suggestion_id}/approve        approve_merge_suggestion                   [WRITE] deps=['get_db', 'get_current_user']
       Approve and execute a merge suggestion.
POST   /admin/merge-suggestions/{suggestion_id}/reject         reject_merge_suggestion                    [WRITE] deps=['get_db', 'get_current_user']
       Reject a merge suggestion.
POST   /admin/merge-suggestions/generate-pipeline-aware        generate_pipeline_aware_suggestions        [read] deps=['get_db', "require_role(['admin']"]
       Generate merge suggestions using advanced ML-based pipeline-aware clustering.
POST   /admin/merge-suggestions/training-jobs                  create_training_job                        [read audit] deps=['get_db', "require_role(['admin']", '_require_ml_csrf']
       Training NEVER runs inside this request — it is a staged background
GET    /admin/merge-suggestions/training-jobs/{job_id}         get_training_job                           [read] deps=["require_role(['admin']"]
       Status of one similarity-model training job from task history. Job ids of any other type are 404 — this endpoi
GET    /admin/merge-suggestions/training-jobs                  list_training_jobs                         [WRITE] deps=["require_role(['admin']"]
       Recent training history (Background Tasks records).
POST   /admin/merge-suggestions/training-jobs/{job_id}/cancel  cancel_training_job                        [read audit] deps=["require_role(['admin']", '_require_ml_csrf']
       Best-effort cancellation between training stages.
POST   /admin/merge-suggestions/models/{model_id}/activate     activate_similarity_model                  [read audit] deps=['get_db', "require_role(['admin']", '_require_ml_csrf']
       Atomic activation: gates + artifact hash + load test, then archive
POST   /admin/merge-suggestions/models/{model_id}/reject       reject_similarity_model                    [read audit] deps=['get_db', "require_role(['admin']", '_require_ml_csrf']
       Reject a candidate similarity model with an optional reason (audited). 409 when the model is not in a rejectab
POST   /admin/merge-suggestions/models/{model_id}/rollback     rollback_similarity_model                  [read audit] deps=['get_db', "require_role(['admin']", '_require_ml_csrf']
       Rollback = activate a previously-active archived version (revalidated).
GET    /admin/merge-suggestions/models                         list_similarity_models                     [WRITE] deps=['get_db', "require_role(['admin']"]
       Model registry history (no filesystem paths in the response).
POST   /admin/merge-suggestions/train-model                    train_similarity_model                     [read] deps=['get_db', "require_role(['admin']", '_require_ml_csrf']
       Compatibility shim — training no longer runs inside the request.
GET    /admin/merge-suggestions/model-status                   get_model_status                           [read] deps=['get_db', "require_role(['admin']"]
       Strictly-typed status. Sample counts come from the PERSISTENT dataset
```

### `backend/routes/intelligence.py`

```
GET    /api/identities/{identity_id}/related                   get_related_identities                     [read audit] deps=['get_db', 'require_admin(']
       Get identities that frequently appear with this identity.
POST   /api/identities/{identity_id}/related/refresh           refresh_relationships                      [read audit] deps=['get_db', 'require_admin(', 'require_intel_csrf']
       Refresh relationship cache for an identity.
GET    /api/identities/{identity_id}/temporal-patterns         get_temporal_patterns                      [read audit] deps=['get_db', 'require_admin(']
       Get temporal patterns for an identity.
GET    /api/identities/{identity_id}/cross-camera              get_cross_camera_track                     [read audit] deps=['get_db', 'require_admin(']
       Get cross-camera tracking for an identity.
GET    /api/identities/{identity_id}/map-data                  get_tracking_map_data                      [read audit] deps=['get_db', 'require_admin(']
       Map data as GeoJSON for MapLibre.
GET    /api/maps/availability                                  get_map_availability                       [read] deps=['get_current_user']
       
POST   /api/maps/verify                                        verify_map_datasets                        [read] deps=['require_admin(', 'require_intel_csrf']
       
GET    /api/identities/{identity_id}/timeline                  get_movement_timeline                      [read audit] deps=['get_db', 'require_admin(']
       Get movement timeline for an identity.
GET    /api/identities/{identity_id}/analyze                   analyze_identity                           [read audit] deps=['get_db', 'require_admin(', "rate_limited('identity_analysis', heavy=True"]
       Get complete analysis for an identity with per-section statuses.
GET    /api/security/network                                   get_social_network                         [read audit] deps=['get_db', 'require_admin(', "rate_limited('network_analysis', heavy=True"]
       Get social network analysis — always bounded.
GET    /api/security/patterns                                  get_suspicious_patterns                    [WRITE audit] deps=['get_db', 'require_admin(', "rate_limited('pattern_detection', heavy=True"]
       Detect suspicious patterns — enveloped so truncation is visible.
GET    /api/security/anomalies/{identity_id}                   get_anomalies                              [read audit] deps=['get_db', 'require_admin(', "rate_limited('anomaly_analysis', heavy=True"]
       Detect anomalies — enveloped so 'no anomalies' and 'not enough
GET    /api/security/threat/{identity_id}                      get_threat_assessment                      [WRITE audit] deps=['get_db', 'require_admin(', "rate_limited('threat_assessment', heavy=True"]
       Get threat assessment for an identity.
POST   /api/intelligence/relationships/calculate-all           calculate_all_relationships_endpoint       [read audit] deps=['require_admin(', 'require_intel_csrf', "rate_limited('relationship_recalc', heavy=True"]
       Schedule the calculate-all background job (202 + job_id).
GET    /api/intelligence/relationships/jobs/{job_id}           get_relationship_job                       [read] deps=['require_admin(']
       Return task-history record for a relationship job.
POST   /api/intelligence/thresholds/learn                      learn_thresholds                           [WRITE] deps=['get_db', 'require_admin(', 'require_intel_csrf', "rate_limited('threshold_learning', heavy=True"]
       DEPRECATED synchronous variant — prefer POST /api/intelligence/thresholds/jobs.
POST   /api/intelligence/thresholds/jobs                       create_threshold_job                       [read audit] deps=['require_admin(', 'require_intel_csrf', "rate_limited('threshold_learning', heavy=True"]
       Schedule threshold learning in the background — single-flight
GET    /api/intelligence/thresholds/jobs/{job_id}              get_threshold_job                          [read] deps=['require_admin(']
       Status of one threshold-learning job. While the deprecated synchronous path holds the guard, its id answers wi
GET    /api/security/capabilities                              get_security_capabilities                  [read] deps=['require_admin(', 'get_db']
       Honest feature-status report used by the frontend status dialog.
GET    /api/intelligence/trajectory/predict                    predict_next_camera                        [read audit] deps=['get_db', 'require_admin(', "rate_limited('trajectory', heavy=True"]
       Predict next camera locations for an identity.
GET    /api/intelligence/correlation/calculate                 calculate_activity_correlation             [read audit] deps=['get_db', 'require_admin(', "rate_limited('correlation', heavy=True"]
       Calculate activity association between two identities.
```

### `backend/routes/live_alerts.py`

```
GET    /api/live-alerts                                        list_live_alerts                           [read] deps=['get_db', 'require_unknown_faces_access(']
       Get all live alerts for current user.
POST   /api/live-alerts                                        create_live_alert                          [WRITE audit] deps=['get_db', 'require_unknown_faces_access(']
       Create a new live alert (identity access enforced server-side).
GET    /api/live-alerts/defaults/{identity_id}                 get_default_alert_settings                 [WRITE] deps=['get_db', 'require_unknown_faces_access(']
       Get default settings for creating a live alert (access-checked).
GET    /api/live-alerts/test-jobs/{job_id}                     get_channel_test_job                       [read] deps=['require_admin(']
       Poll an asynchronous notification-channel test job (admin only).
GET    /api/live-alerts/{alert_id}                             get_live_alert                             [read] deps=['get_db', 'require_unknown_faces_access(']
       Get a specific live alert (owner or admin only).
PUT    /api/live-alerts/{alert_id}                             update_live_alert                          [read audit] deps=['get_db', 'require_unknown_faces_access(']
       Update a live alert (owner or admin only).
DELETE /api/live-alerts/{alert_id}                             delete_live_alert                          [WRITE audit] deps=['get_db', 'require_unknown_faces_access(']
       Delete a live alert (owner or admin only).
POST   /api/live-alerts/{alert_id}/pause                       pause_live_alert                           [read audit] deps=['get_db', 'require_unknown_faces_access(']
       Pause a live alert (owner or admin only).
POST   /api/live-alerts/{alert_id}/resume                      resume_live_alert                          [read audit] deps=['get_db', 'require_unknown_faces_access(']
       Resume a paused alert (owner or admin only).
GET    /api/live-alerts/{alert_id}/triggers                    get_alert_triggers                         [read] deps=['get_db', 'require_unknown_faces_access(']
       Server-side paginated + filtered trigger history (owner or admin only).
POST   /api/live-alerts/{alert_id}/triggers/acknowledge-all    acknowledge_all_triggers                   [read audit] deps=['get_db', 'require_unknown_faces_access(']
       Acknowledge all (or the listed) unacknowledged triggers in ONE
POST   /api/live-alerts/triggers/{trigger_id}/acknowledge      acknowledge_trigger                        [read audit] deps=['get_db', 'require_unknown_faces_access(']
       Acknowledge a trigger (owner of the parent alert or admin only).
GET    /api/live-alerts/{alert_id}/health                      get_alert_health                           [WRITE] deps=['get_db', 'require_unknown_faces_access(']
       
POST   /api/live-alerts/{alert_id}/test                        test_alert_channels                        [read audit] deps=['get_db', 'require_admin(']
       Admin only: asynchronously test the alert's notification channels.
```

### `backend/routes/logs.py`

```
GET    /api/logs/config                                        get_logs_config                            [read] deps=["require_role(['admin']"]
       Bounds and vocabulary for the log viewer.
GET    /api/logs                                               get_logs                                   [read] deps=["require_role(['admin']"]
       Read the application log from the rotated files on disk, newest first, filtered by level and date. The scan is
GET    /api/logs/stats                                         get_log_stats                              [read] deps=["require_role(['admin']"]
       Counts by level over the configured log set, within the same bounds.
POST   /api/logs/cleanup                                       manual_log_cleanup                         [read] deps=["require_role(['admin']"]
       Apply `LOGS_LIFE_TIME_HOURS` to the configured log set.
```

### `backend/routes/management.py`

```
POST   /api/clustering/run                                     clustering_run_now                         [read] deps=[]
       Run identity clustering now instead of waiting for its startup delay.
POST   /api/identity-retention/run                             identity_retention_run_now                 [read] deps=[]
       Run the identity retention cleanup now (same cycle the daily timer runs).
POST   /api/cleanup/manual                                     manual_cleanup                             [read] deps=[]
       Manually trigger cleanup of old data
GET    /api/circuit-breaker/status                             circuit_breaker_status                     [read] deps=[]
       Get circuit breaker status
GET    /api/face-tracker/stats                                 face_tracker_stats_fixed                   [read] deps=[]
       Get face tracker statistics (FIXED)
POST   /api/face-tracker/reset/{pipeline_id}                   reset_face_tracker                         [read] deps=[]
       Reset face tracker for a specific pipeline
```

### `backend/routes/metrics.py`

```
GET    /metrics                                                metrics                                    [read] deps=[]
       Prometheus metrics endpoint.
```

### `backend/routes/ml_ops.py`

```
GET    /api/ml/capabilities                                    platform_capabilities                      [WRITE] deps=['ML_MANAGE']
       
GET    /api/ml/pipelines                                       list_pipeline_versions                     [WRITE] deps=['get_db', 'ML_MANAGE']
       
POST   /api/ml/pipelines                                       create_pipeline_version                    [WRITE audit] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf', "rate_limited('ml_ops'"]
       
GET    /api/ml/experiments                                     list_experiments                           [WRITE] deps=['get_db', 'ML_MANAGE']
       
GET    /api/ml/comparisons                                     compare_experiments                        [WRITE] deps=['get_db', 'ML_MANAGE']
       
POST   /api/ml/experiments/{job_id}/retry                      retry_tracking                             [WRITE audit] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf', "rate_limited('ml_ops', heavy=True"]
       
POST   /api/ml/models/{model_id}/promote                       promote_offline_model                      [read] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf', "rate_limited('ml_ops'"]
       
GET    /api/ml/models/{model_id}/explanations                  model_explanations                         [read] deps=['get_db', 'ML_MANAGE']
       
GET    /api/ml/models/{model_id}/explanations/download/{filename} download_explanation                       [read] deps=['get_db', 'ML_MANAGE']
       
GET    /api/ml/overview                                        ml_overview                                [WRITE] deps=['get_db', 'ML_MANAGE']
       One read-only payload for the ML Ops dashboard: mode availability with unmet gates, label stats, feature/predi
PUT    /api/ml/config/mode                                     change_mode                                [read] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf', "rate_limited('ml_ops', heavy=False"]
       Change the ML decision mode. Refused with 409 MODE_GATED while any promotion gate is unmet; on success the mod
POST   /api/ml/pause                                           pause_ml                                   [read] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf']
       Emergency stop: unconditionally forces the decision mode back to 'rules', bypassing gate checks. Persisted, ap
POST   /api/ml/features/compute                                compute_features                           [WRITE audit] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf', "rate_limited('ml_ops', heavy=True"]
       Persist a feature-snapshot command for the independent ML worker.
GET    /api/ml/features/definitions                            feature_definitions                        [WRITE] deps=['get_db', 'ML_MANAGE']
       List every feature definition (active first): name, version, entity type, window, leakage class and readiness 
GET    /api/ml/labels/stats                                    label_stats                                [read] deps=['get_db', 'ML_MANAGE']
       Label counts by class and review status, the configured supervised-training minimums, and whether the supervis
GET    /api/ml/labels                                          list_labels                                [read] deps=['get_db', 'ML_MANAGE']
       Paginated label listing, newest first, filterable by class, kind, review status and subject.
POST   /api/ml/labels                                          create_label                               [read] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf', "rate_limited('ml_ops', heavy=False"]
       Create a label (idempotent — a duplicate returns 200 instead of 201; weak labels are capped below full confide
POST   /api/ml/labels/{label_id}/review                        review_label                               [read] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf']
       Confirm, dispute or retract a label. Only confirmed manual labels count toward the supervised gate. Illegal tr
POST   /api/ml/labels/{label_id}/supersede                     supersede_label                            [read] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf']
       Correct a label WITHOUT editing history: the old label becomes `superseded`, a new active label is created wit
GET    /api/ml/datasets                                        list_datasets                              [WRITE] deps=['get_db', 'ML_MANAGE']
       Paginated dataset-version listing, newest first.
POST   /api/ml/datasets                                        build_dataset_endpoint                     [WRITE audit] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf', "rate_limited('ml_ops', heavy=True"]
       Validate and enqueue an immutable dataset build; execution is worker-owned.
GET    /api/ml/datasets/definitions                            list_dataset_definitions                   [read] deps=['ML_MANAGE']
       The typed extraction definitions a dataset can be built from, with the known limitations of the feature set ea
POST   /api/ml/datasets/backfill-hashes                        backfill_dataset_hashes_endpoint           [WRITE audit] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf', "rate_limited('ml_ops', heavy=True"]
       Enqueue verification of legacy dataset files and their logical checksums.
POST   /api/ml/datasets/{dataset_id}/archive                   archive_dataset_endpoint                   [WRITE audit] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf', "rate_limited('ml_ops', heavy=False"]
       Releases the Parquet bytes of a dataset no registered model was trained from. The row (provenance) and manifes
GET    /api/ml/datasets/{dataset_id}/explorer                  dataset_explorer                           [WRITE] deps=['get_db', 'ML_MANAGE']
       Bounded artifact preview; downloads contain the original validation report.
GET    /api/ml/datasets/{dataset_id}/validation-report         dataset_explorer                           [WRITE] deps=['get_db', 'ML_MANAGE']
       Bounded artifact preview; downloads contain the original validation report.
GET    /api/ml/datasets/{dataset_id}                           dataset_detail                             [WRITE] deps=['get_db', 'ML_MANAGE']
       One dataset version with its extraction record, manifest-derived lineage, feature-set limitations and the mode
POST   /api/ml/training-jobs                                   create_training_job                        [WRITE audit] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf', "rate_limited('ml_ops', heavy=True"]
       Persist training for the independent worker; never execute it in the API.
POST   /api/ml/score/relational                                score_relational_model                     [WRITE audit] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf', "rate_limited('ml_ops', heavy=True"]
       Score a pair or graph node without changing any live decision.
POST   /api/ml/rank/threat-review                              rank_threat_review                         [WRITE audit] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf', "rate_limited('ml_ops', heavy=True"]
       Offline analyst prioritisation only; never writes a threat decision.
GET    /api/ml/jobs                                            list_ml_jobs_endpoint                      [read] deps=['get_db', 'ML_MANAGE']
       Reconnectable queue state. Status is a comma-separated allowlist.
GET    /api/ml/training-jobs/{job_id}                          get_training_job                           [read] deps=['ML_MANAGE']
       Status of one durable ML job. The training-jobs path is retained for compatibility.
GET    /api/ml/jobs/{job_id}                                   get_training_job                           [read] deps=['ML_MANAGE']
       Status of one durable ML job. The training-jobs path is retained for compatibility.
POST   /api/ml/training-jobs/{job_id}/cancel                   cancel_training_job                        [WRITE audit] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf']
       Persist cancellation so it survives API/worker process boundaries.
POST   /api/ml/jobs/{job_id}/cancel                            cancel_training_job                        [WRITE audit] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf']
       Persist cancellation so it survives API/worker process boundaries.
GET    /api/ml/models                                          list_models                                [WRITE] deps=['get_db', 'ML_MANAGE']
       Paginated model registry listing, newest first, filterable by model type and stage.
GET    /api/ml/models/{model_id}                               get_model                                  [read] deps=['get_db', 'ML_MANAGE']
       One model with its decision-threshold SETS (one row per scope/version:
POST   /api/ml/models/{model_id}/readiness                     recompute_model_readiness                  [WRITE audit] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf', "rate_limited('ml_ops', heavy=True"]
       Recompute BOTH readiness gates for a registered model from its artifact,
POST   /api/ml/models/{model_id}/shadow-approve                shadow_approve                             [read] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf', "rate_limited('ml_ops', heavy=True"]
       Explicit administrator approval — the ONLY path into shadow. The full
POST   /api/ml/models/{model_id}/reject                        reject_model                               [read] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf']
       Move a model to the rejected stage with a recorded reason. Every registry error (unknown model, illegal transi
POST   /api/ml/shadow/stop                                     stop_shadow                                [read] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf']
       The rollback drill: archives the shadow model. Live decisions were
GET    /api/ml/predictions                                     list_predictions                           [WRITE] deps=['get_db', 'ML_MANAGE']
       Paginated prediction log, newest first: requested vs actual mode, fallback reason, score and band, calibration
GET    /api/ml/shadow/summary                                  shadow_summary                             [read] deps=['get_db', 'ML_MANAGE']
       Descriptive shadow-vs-rules aggregates over the last N days (1-90): disagreement counts, band distribution by 
GET    /api/ml/shadow/evidence                                 shadow_evidence                            [read] deps=['get_db', 'ML_MANAGE']
       Per model/band: predictions, reviewed manual outcomes and their split, rule-severity x band crosstab, disagree
GET    /api/ml/drift/reports                                   drift_reports                              [WRITE] deps=['get_db', 'ML_MANAGE']
       Paginated drift reports, newest first, optionally for one model. Drift reports are informational only — they n
POST   /api/ml/drift/run                                       run_drift                                  [WRITE audit] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf', "rate_limited('ml_ops', heavy=True"]
       Enqueue report-only drift computation for the independent worker.
GET    /api/ml/retraining-policy/{model_type}                  get_policy                                 [WRITE] deps=['get_db', 'ML_MANAGE']
       The retraining policy for one model type. Scheduled retraining is scaffolded and disabled in this release.
PUT    /api/ml/retraining-policy/{model_type}                  update_policy                              [WRITE audit] deps=['get_db', 'ML_MANAGE', 'require_mlops_csrf']
       Update retraining-policy parameters. Enabling scheduled retraining is refused with 409 SCHEDULED_RETRAINING_GA
GET    /api/ml/calls                                           ml_call_log                                [read] deps=['ML_MANAGE']
       Newest-first records of recent /api/ml/* calls (request id, actor, method, route, status, error code, duration
GET    /api/ml/audit                                           ml_audit_log                               [WRITE audit] deps=['get_db', 'ML_MANAGE']
       Paginated ML audit trail (action, object, actor, reason), newest first. The stored before/after payloads are n
```

### `backend/routes/retention.py`

```
GET    /status                                                 retention_status                           [WRITE] deps=['get_db', "require_role(['admin']"]
       Retention configuration + last/next run report.
POST   /run                                                    retention_run                              [WRITE audit] deps=['get_db', "require_role(['admin']"]
       Schedule a retention run as a background job and return immediately.
```

### `backend/routes/risk_assessments.py`

```
POST   /api/security/assessments                               create_assessment                          [read audit] deps=['get_db', 'require_admin(', 'require_intel_csrf', "rate_limited('assessment_recalc', heavy=True"]
       
GET    /api/security/assessments                               list_assessments                           [read audit] deps=['get_db', 'require_admin(']
       Paginated risk assessments filtered by person, pipeline, location, severity, status and creation-date range. I
GET    /api/security/assessments/history/{subject_type}/{subject_id} assessment_history                         [read audit] deps=['get_db', 'require_admin(']
       Assessment history for one identity (the only supported subject type), paginated. The identity must exist.
GET    /api/security/assessments/{assessment_id}               get_assessment                             [read audit] deps=['get_db', 'require_admin(']
       One assessment, including its score type, probability flag and calibration status.
POST   /api/security/assessments/{assessment_id}/acknowledge   acknowledge_assessment                     [read] deps=['get_db', 'require_admin(', 'require_intel_csrf']
       Mark an open assessment acknowledged, recording who and when. A resolved assessment must be reopened first (40
POST   /api/security/assessments/{assessment_id}/resolve       resolve_assessment                         [read audit] deps=['get_db', 'require_admin(', 'require_intel_csrf']
       Resolve an assessment with a resolution status and optional notes, back-filling acknowledgement if it was neve
POST   /api/security/assessments/{assessment_id}/reopen        reopen_assessment                          [read] deps=['get_db', 'require_admin(', 'require_intel_csrf']
       Reopen a resolved assessment (anything not currently resolved is 409). Status returns to open with resolution_
GET    /api/security/risk-model                                get_risk_model                             [WRITE] deps=['get_db', 'require_admin(']
       
GET    /api/security/learned-thresholds                        list_learned_thresholds                    [read] deps=['get_db', 'require_admin(']
       Learned decision thresholds (up to 200), filterable by signal and status, each with value, sample count, versi
POST   /api/security/learned-thresholds/{threshold_id}/activate activate_learned_threshold                 [WRITE audit] deps=['get_db', 'require_admin(', 'require_intel_csrf']
       
```

### `backend/routes/settings.py`

```
GET    /                                                       get_all_settings                           [WRITE] deps=['get_db', "require_role(['admin']"]
       Get all system settings with typed metadata (stored vs effective value,
GET    /categories                                             get_categories                             [WRITE] deps=['get_db', "require_role(['admin']"]
       Get all setting categories.
GET    /audit/log                                              get_audit_log                              [WRITE audit] deps=['get_db', "require_role(['admin']"]
       Get settings audit log with formatted data.
GET    /{setting_key}                                          get_setting                                [WRITE] deps=['get_db', "require_role(['admin']"]
       Get a specific setting by key with full typed metadata
PUT    /{setting_key}                                          update_setting                             [WRITE audit] deps=['get_db', "require_role(['admin']", 'require_settings_csrf']
       Update a setting value with audit logging.
```

### `backend/routes/sso.py`

```
GET    /api/sso/laf-ai/launch                                  launch                                     [read audit] deps=['require_chatbot_access(']
       What TRACKING points at: mint a ticket and send the browser to the chatbot.
POST   /api/sso/laf-ai/ticket                                  create_ticket                              [read audit] deps=['require_chatbot_access(', 'require_auth_csrf']
       Same ticket as `launch`, returned as JSON for scripted clients and tests.
POST   /api/sso/laf-ai/consume                                 consume                                    [WRITE audit] deps=['get_db']
       Redeem a ticket exactly once. Internal to the chatbot gate: calls that
```

### `backend/routes/stats.py`

```
GET    /api/stats                                              get_stats                                  [WRITE] deps=['get_db', 'get_current_user']
       Get system statistics - filtered by the caller's pipeline access.
GET    /api/dashboard/config                                   get_dashboard_config                       [read] deps=[]
       Get dashboard configuration settings.
GET    /api/dashboard/pipelines                                get_dashboard_pipelines                    [WRITE] deps=['get_db', 'get_current_user']
       Authoritative pipeline reconciliation endpoint for the dashboard.
```

### `backend/routes/task_history.py`

```
GET    /api/tasks/stats                                        get_task_stats                             [read] deps=['get_db', "require_role(['admin']"]
       
GET    /api/tasks/history                                      get_task_history                           [read] deps=['get_db', 'get_current_user']
       
GET    /api/tasks/alerts                                       get_task_alerts                            [read] deps=['get_db', 'get_current_user']
       
GET    /api/tasks/upcoming                                     get_upcoming_tasks                         [read] deps=['get_db', 'get_current_user']
       Scheduled tasks whose scheduled_time is in the future (excludes overdue).
GET    /api/tasks/running                                      get_running_tasks                          [read] deps=['get_db', 'get_current_user']
       Currently running background tasks (up to 50), newest first, as a bare JSON array. Non-admin callers get error
GET    /api/tasks/{task_id}                                    get_task_detail                            [read] deps=['get_db', 'get_current_user']
       
POST   /api/tasks/{task_id}/cancel                             cancel_task                                [read] deps=['get_db', "require_role(['admin']"]
       
POST   /api/tasks/{task_id}/retry                              retry_task                                 [read] deps=['get_db', "require_role(['admin']"]
       
```

### `backend/routes/upload.py`

```
POST   /api/identities/{identity_id}/images                    add_identity_image                         [read] deps=["require_role(['admin']", 'require_upload_csrf', 'get_db']
       Adds one more photo (and one more embedding) to a person that exists.
GET    /api/identities/{identity_id}/images                    list_identity_images                       [WRITE] deps=["require_role(['admin']", 'get_db']
       All enrollment images of an identity, primary first. A well-formed but unknown UUID returns total: 0 rather th
PUT    /api/identities/{identity_id}/images/{image_id}/primary make_image_primary                         [read] deps=["require_role(['admin']", 'require_upload_csrf', 'get_db']
       An administrator decides which photo represents this person.
POST   /api/upload-person                                      upload_person                              [WRITE audit] deps=["require_role(['admin']", 'require_upload_csrf', 'get_db']
       Enroll a person by NAME, creating them when they do not exist yet.
```

### `backend/routes/users.py`

```
POST   /api/users                                              create_user                                [WRITE] deps=['get_db', "require_role(['admin']", 'require_user_admin_csrf']
       Create a new user (admin only)
GET    /api/users                                              list_users                                 [WRITE] deps=['get_db', "require_role(['admin']"]
       List all users (admin only)
GET    /api/users/{user_id}                                    get_user                                   [WRITE] deps=['get_db', "require_role(['admin']"]
       Get user by ID (admin only)
PUT    /api/users/{user_id}                                    update_user                                [WRITE] deps=['get_db', "require_role(['admin']", 'require_user_admin_csrf']
       Update user (admin only)
POST   /api/users/{user_id}/reset-password                     reset_password                             [read] deps=['get_db', "require_role(['admin']", 'require_user_admin_csrf']
       Reset user password (admin only)
DELETE /api/users/{user_id}                                    delete_user                                [WRITE] deps=['get_db', "require_role(['admin']", 'require_user_admin_csrf']
       Permanently delete a user (admin only), preserving history.
POST   /api/users/system/restore                               restore_system_principal                   [read] deps=['get_db', "require_role(['admin']", 'require_user_admin_csrf']
       Recreate or repair the protected `system` audit principal (admin only).
GET    /api/users/me/pipelines                                 get_my_pipelines                           [WRITE] deps=['get_db', 'get_current_user']
       Get current user's accessible pipelines
POST   /api/users/{user_id}/unblock                            unblock_user                               [WRITE] deps=['get_db', "require_role(['admin']", 'require_user_admin_csrf']
       Unblock a user (admin only) - restores system access
GET    /api/pipelines                                          get_all_pipelines                          [WRITE] deps=['get_db', 'get_current_user']
       Get pipelines accessible to the current user.
PUT    /api/pipelines/{pipeline_id}/coordinates                update_pipeline_coordinates                [WRITE] deps=['get_db', "require_role(['admin']"]
       Update pipeline coordinates (latitude, longitude, location_name)
PUT    /api/pipelines/{pipeline_id}/rename                     rename_pipeline                            [WRITE] deps=['get_db', "require_role(['admin']"]
       Rename a pipeline (e.g. a tool-generated UUID -> a friendly camera name).
```

### `backend/routes/utils.py`

```
```

### `backend/routes/watchlists.py`

```
GET    /api/watchlists                                         list_watchlists                            [read] deps=['get_db', 'require_admin(']
       List watchlists with batched statistics (no N+1).
POST   /api/watchlists                                         create_watchlist                           [read audit] deps=['get_db', 'require_admin(', 'require_watchlist_csrf']
       Create a new watchlist (409 on duplicate live name).
GET    /api/watchlists/{watchlist_id}                          get_watchlist                              [read audit] deps=['get_db', 'require_admin(']
       Get a specific watchlist with real statistics.
PUT    /api/watchlists/{watchlist_id}                          update_watchlist                           [read audit] deps=['get_db', 'require_admin(', 'require_watchlist_csrf']
       Update a watchlist with optimistic concurrency (409 on stale version).
PATCH  /api/watchlists/{watchlist_id}/status                   change_watchlist_status                    [read audit] deps=['get_db', 'require_admin(', 'require_watchlist_csrf']
       Explicit activation state change with reason + audit.
GET    /api/watchlists/{watchlist_id}/deletion-impact          get_deletion_impact                        [read] deps=['get_db', 'require_admin(']
       
DELETE /api/watchlists/{watchlist_id}                          delete_watchlist                           [WRITE audit] deps=['get_db', 'require_admin(', 'require_watchlist_csrf']
       Soft delete (default) or explicitly confirmed hard delete.
POST   /api/watchlists/{watchlist_id}/restore                  restore_watchlist                          [read audit] deps=['get_db', 'require_admin(', 'require_watchlist_csrf']
       Restore a soft-deleted watchlist.
GET    /api/watchlists/{watchlist_id}/stats                    get_watchlist_stats                        [read] deps=['get_db', 'require_admin(']
       Statistics with an explicit reporting period.
GET    /api/watchlists/{watchlist_id}/entries                  list_watchlist_entries                     [read] deps=['get_db', 'require_admin(']
       Paginated entries for a watchlist.
POST   /api/watchlists/{watchlist_id}/entries                  add_to_watchlist                           [WRITE audit] deps=['get_db', 'require_admin(', 'require_watchlist_csrf']
       Add an identity to a watchlist (duplicate-safe).
DELETE /api/watchlists/{watchlist_id}/entries/{identity_id}    remove_from_watchlist                      [WRITE audit] deps=['get_db', 'require_admin(', 'require_watchlist_csrf']
       Remove an identity from a watchlist.
GET    /api/identities/{identity_id}/watchlists                get_identity_watchlists                    [read] deps=['get_db', 'require_admin(']
       Get all watchlists an identity is on.
GET    /api/watchlists/add-identity/{identity_id}/defaults     get_add_to_watchlist_defaults              [WRITE] deps=['get_db', 'require_admin(']
       Available watchlists + defaults for the add-identity dialog.
GET    /api/watchlist-alerts                                   list_alerts                                [read] deps=['get_db', 'require_admin(']
       Get watchlist alerts.
POST   /api/watchlist-alerts/{alert_id}/acknowledge            acknowledge_alert                          [read audit] deps=['get_db', 'require_admin(', 'require_watchlist_csrf']
       Acknowledge a watchlist alert.
```

### `backend/routes/webhook.py`

```
POST   /webhook/{pipeline_id}                                  webhook                                    [read] deps=[]
       Webhook endpoint - /webhook/{pipeline_id}
POST   /api/webhook/{pipeline_id}                              webhook_api                                [read] deps=[]
       Webhook endpoint - /api/webhook/{pipeline_id} (alias for compatibility)
GET    /webhook/test                                           webhook_test                               [read] deps=[]
       Verify that ingest routes are reachable AND that your key is accepted.
GET    /api/webhook/test                                       webhook_test                               [read] deps=[]
       Verify that ingest routes are reachable AND that your key is accepted.
GET    /api/webhook/images/{pipeline_id}                       list_webhook_images                        [read] deps=["require_role(['admin']"]
       List all saved webhook images for a pipeline.
GET    /api/webhook/images/{pipeline_id}/{filename}            get_webhook_image                          [read] deps=["require_role(['admin']"]
       Serve one debug snapshot, anchored inside the configured base.
```

### `backend/routes/webhook_credentials.py`

```
DELETE /{credential_id}                                        revoke_credential                          [WRITE audit] deps=['get_db', "require_role(['admin']", 'require_upload_csrf']
       Revoke by deleting the row. There is no revoked state to distinguish.
```

### `backend/routes/websocket.py`

```
WEBSOCKET /ws                                                     websocket_endpoint                         [WRITE] deps=[]
       WebSocket for real-time dashboard updates
```

### `sql_agent/api/routes.py`

```
POST   /query                                                  sql_agent_query                            [read] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       Query the SQL Intelligence Agent with natural language.
POST   /query/stream                                           sql_agent_query_stream                     [read] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None', 'get_db) if AUTH_AVAILABLE else None']
       Query the SQL Intelligence Agent with streaming SSE response.
GET    /requests/{request_id}/trace                            get_sql_agent_trace                        [WRITE] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       Owner-only operational evidence. No prompts, raw rows or private reasoning.
POST   /requests/{request_id}/cancel                           cancel_sql_agent_request                   [read] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None', 'require_sql_agent_csrf']
       Server-side cancellation: stops the LLM/SQL worker thread at its next
GET    /health                                                 sql_agent_health                           [read] deps=[]
       Lightweight component health: never invokes the LLM or heavy SQL.
GET    /schema                                                 sql_agent_schema                           [read] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       Get database schema description.
POST   /session/new                                            sql_agent_new_session                      [read] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       Start a new conversation session for the calling user.
GET    /sessions                                               sql_agent_list_sessions                    [read] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       List the calling user's conversation sessions.
POST   /session/load                                           sql_agent_load_session                     [read] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       Load one of the calling user's own sessions.
POST   /export/pdf                                             export_to_pdf                              [read] deps=['require_chatbot_access(', 'require_sql_agent_csrf']
       Export chatbot response to PDF format (auth + CSRF + size limits +
POST   /export/word                                            export_to_word                             [read] deps=['require_chatbot_access(', 'require_sql_agent_csrf']
       Export chatbot response to Word (auth + CSRF + size limits + bounded
GET    /artifacts/{artifact_id}                                download_artifact                          [read] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       Download a document the agent generated, by id.
GET    /history                                                get_query_history                          [read] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       User's query history — server-side pagination, owner-scoped.
GET    /history/{query_id}                                     get_query_by_id                            [read] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       Get a specific query by ID.
DELETE /history/{query_id}                                     delete_query_from_history                  [WRITE] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None', 'require_sql_agent_csrf']
       Permanently delete one of the CALLER'S OWN history entries (the service
GET    /sessions/list                                          list_user_sessions                         [read] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       List user's conversation sessions.
POST   /sessions/create                                        create_session                             [WRITE] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       Create a new conversation session.
PUT    /sessions/{session_id}                                  update_session                             [WRITE] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       Update a session (e.g., context summary, active status).
GET    /memory                                                 get_user_memories                          [read] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       Get user's memories.
POST   /memory                                                 create_memory                              [WRITE] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       Create a new memory.
DELETE /memory/{memory_id}                                     delete_memory                              [WRITE] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       Delete a memory.
GET    /context                                                get_query_context                          [read] deps=['require_chatbot_access()) if AUTH_AVAILABLE else None']
       Get context for AI agent (recent queries + memories).
```
